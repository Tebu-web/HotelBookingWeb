@page
@model HotelBookingWeb.Pages.Recenzii.IndexModel

@{
    ViewData["Title"] = "Recenzii";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Recenzie[0].Continut)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Recenzie[0].Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Recenzie[0].DataRecenzie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Recenzie[0].Client)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Recenzie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Continut)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataRecenzie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Client.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.RecenzieId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.RecenzieId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.RecenzieId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
